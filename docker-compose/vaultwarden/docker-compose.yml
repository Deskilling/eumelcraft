services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: always
    environment:
      DOMAIN: https://www.yourdomain.com
    volumes:
      - ./vw-data/:/data/
    ports:
      - 10380:80
networks: {}
